version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: proflex
      POSTGRES_PASSWORD: password # Change this on the server!
    volumes:
      - ../database/sql_scripts:/sql_scripts
      - ./app/postgres-data:/var/lib/postgresql/data

  webshop:
    image: ghcr.io/nokacper24/group4webshop:main
    ports:
      - "443:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      ALLOWED_ORIGINS: https://group04.web-tek.ninja
      DATABASE_URL: postgresql://backend_user:PASSWORD@db:5432/proflex # Change PASSWORD on the server!
      CERT_PATH: /certs/live/group04.web-tek.ninja/cert.pem
      PRIV_KEY_PATH: /certs/live/group04.web-tek.ninja/privkey.pem
    depends_on:
      - db
    volumes:
      - ./app/resources:/resources
      - /etc/letsencrypt/live/group04.web-tek.ninja:/certs/live/group04.web-tek.ninja
      - /etc/letsencrypt/archive/group04.web-tek.ninja:/certs/archive/group04.web-tek.ninja
    restart: always

  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - webshop