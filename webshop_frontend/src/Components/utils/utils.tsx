import "./utils.css";

/**
 * Returns a div containing a loading spinner.
 *
 * @returns {HTMLDivElement} div containing a loading spinner
 */
export default function LoadingSpinner() {
  return <div className="loading-spinner" />;
}
